function love.conf(t) -- to config app more
    -- t.identity = "data/saves" -- assign a save file
    -- t.version = "1.0.0" -- version of a game
    -- t.console = false
    -- t.externalstorage = true -- save file int external storge -- android only
    -- t.gammacorrect = true -- brightness
    -- t.audio.mic = true -- enable mic
    t.window.title = "Cool Ball Game" -- title of game
    t.window.icon = "assets/logo.jpeg" -- game icon
    -- t.window.width = 1080 
    -- t.window.height = 720 
    -- t.window.resizable = true -- you can resize the window -- it can break the window
    -- t.window.minwidth = 500 
    -- t.window.minheight = 200 
    -- t.window.borderless = true -- remove border
    -- t.window.vsync = 1 -- sync sound and game
    -- t.window.display = 2 -- display new window over the existing one
    -- t.window.fullscreen = true
    -- t.window.x = 200
    -- t.window.y = 100
    -- t.modules.timer = false -- don't use this
end